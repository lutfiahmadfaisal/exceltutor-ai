import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ExcelTutor AI — Generator Video Tutorial Excel Otomatis",
  description:
    "Hasilkan video tutorial Excel otomatis dari prompt teks. Didukung AI, animasi spreadsheet, dan narasi suara.",
  keywords: ["Excel", "tutorial", "AI", "video generator", "VLOOKUP", "IF", "SUMIF"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="id"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
